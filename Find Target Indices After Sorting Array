/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var targetIndices = function(nums, target) {
    var output = []
    nums.sort((a,b)=> a-b)
    nums.forEach((number,index)=>{if(number===target) output.push(index)})
    return output
};
